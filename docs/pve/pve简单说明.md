pve是一个系统，但是它和我们常见的linux或者windows有些区别，它是一个专门为了虚拟化而订做出来的企业级虚拟化平台，说白了就是一个专门用来创建虚拟机的系统，即一个虚拟环境宿主系统。对于性能要求较低，但是服务种类繁多的服务器来说，这个系统将会是极为合适的系统。其他类似的系统还有：`ESXi`

它阉割掉了一些不需要的功能，没有提供图形界面，仅有最基本的命令行界面（类似ubuntu的服务器版本），唯一提供的图形化界面由网络层提供（pve web）。

它是由linux魔改而成的，所以如果你有一定的linux基础，学习该系统的操作将会如鱼得水。

